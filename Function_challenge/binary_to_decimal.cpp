#include <bits/stdc++.h>

using namespace std;
#define endl "\n"
int main()
{
    string binary{};
    scanf("%s", &binary);

    for (int i = 0; i < binary.size(); i++)
    {
        cout << binary[i];
    }

    return 0;
}